[pytest]
pythonpath = src .
testpaths = tests
asyncio_default_fixture_loop_scope = function
addopts = -m "not slow"
markers =
    slow: marks tests as slow (stress/throughput/soak — deselected by default, run with pytest -m slow)
filterwarnings =
    ignore::pytest_asyncio.plugin.PytestDeprecationWarning
